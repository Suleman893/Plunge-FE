//MUI Imports
import { styled } from '@mui/material/styles'

const MaskImg = styled('img')({
  blockSize: '100%',

  // maxBlockSize: 355,
  inlineSize: '100%',
  position: 'absolute',
  insetBlockEnd: 0,
  zIndex: -1
})

export default MaskImg
